includes:
    - phpstan-baseline.neon

parameters:
    level: 3
    paths:
        - src
        - config
    tmpDir: build/phpstan

    excludePaths:
        analyse:
            - vendor/artem_c/emmet

    ignoreErrors:
        - '#Unsafe usage of new static#'
